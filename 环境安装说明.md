# 环境安装说明

## 📦 方法一：一键安装（推荐）

### Windows用户

1. **双击运行** `install_environment.bat` 文件
2. 等待安装完成（可能需要几分钟）
3. 看到"✓ 环境配置完成！"的提示即可

### Linux/Mac用户

1. 打开终端，进入项目目录
2. 运行命令：
   ```bash
   bash install_environment.sh
   ```
3. 等待安装完成

---

## 📦 方法二：手动安装

### 步骤1：检查Python是否安装

打开命令行（Windows按`Win+R`，输入`cmd`；Mac/Linux打开终端），输入：

```bash
python --version
```

或者：

```bash
python3 --version
```

**如果没有安装Python：**
- 访问 https://www.python.org/downloads/
- 下载Python 3.8或更高版本
- 安装时**务必勾选**"Add Python to PATH"（Windows）

### 步骤2：升级pip

```bash
python -m pip install --upgrade pip
```

### 步骤3：安装所有库

在项目根目录下运行：

```bash
pip install -r requirements.txt
```

如果使用python3：

```bash
pip3 install -r requirements.txt
```

---

## 📦 方法三：逐个安装（如果批量安装失败）

如果上面的方法失败，可以逐个安装：

```bash
# 数据处理库
pip install pandas numpy

# 机器学习库
pip install scikit-learn

# 可视化库
pip install matplotlib seaborn

# 内存监控库
pip install memory-profiler psutil

# 进度条库
pip install tqdm

# 科学计算库
pip install scipy
```

---

## ✅ 验证安装

安装完成后，运行以下命令验证：

```bash
python -c "import pandas; import numpy; import sklearn; import matplotlib; import seaborn; import memory_profiler; import psutil; import tqdm; import scipy; print('✓ 所有库安装成功！')"
```

如果看到"✓ 所有库安装成功！"，说明安装完成！

---

## ❌ 常见问题

### 问题1：提示"pip不是内部或外部命令"

**解决方法：**
- 检查Python是否正确安装
- 检查是否勾选了"Add Python to PATH"
- 尝试使用 `python -m pip` 代替 `pip`

### 问题2：安装速度慢或超时

**解决方法：**
- 使用国内镜像源（清华大学镜像）：
  ```bash
  pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
  ```
- 或者使用阿里云镜像：
  ```bash
  pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/
  ```

### 问题3：权限错误（Linux/Mac）

**解决方法：**
- 使用 `--user` 参数：
  ```bash
  pip install --user -r requirements.txt
  ```
- 或者使用 `sudo`（不推荐）：
  ```bash
  sudo pip install -r requirements.txt
  ```

### 问题4：某些库安装失败

**解决方法：**
- 先安装失败的库的依赖（如需要C++编译器的库可能需要安装Visual Studio Build Tools）
- 查看具体错误信息，搜索解决方案
- 尝试更新pip：`python -m pip install --upgrade pip`

---

## 📋 需要安装的库清单

| 库名 | 用途 | 版本要求 |
|------|------|---------|
| pandas | 数据处理 | ≥1.3.0 |
| numpy | 数值计算 | ≥1.21.0 |
| scikit-learn | 机器学习（聚类算法） | ≥1.0.0 |
| matplotlib | 绘图 | ≥3.4.0 |
| seaborn | 高级绘图 | ≥0.11.0 |
| memory-profiler | 内存监控 | ≥0.60.0 |
| psutil | 系统信息 | ≥5.8.0 |
| tqdm | 进度条 | ≥4.62.0 |
| scipy | 科学计算 | ≥1.7.0 |

---

## 🎯 安装完成后

安装完成后，你可以：
1. 继续第三步：认识数据（查看数据集）
2. 开始编写代码进行实验

**祝你实验顺利！** 🎉

